import actions from '../actions';

const openDrawer = actions.openDrawer;
const closeDrawer = actions.closeDrawer;
const toggleDrawer = actions.toggleDrawer;
const toggleDrawerList = actions.toggleDrawerList;

export default {
  openDrawer,
  closeDrawer,
  toggleDrawer,
  toggleDrawerList
};
