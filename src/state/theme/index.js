import Theme from './reducers';

export { default as themeSelectors } from './selectors';
export { default as themeOperations } from './operations';
export { default as themeTypes } from './types';

export default Theme;
